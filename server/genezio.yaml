name: Donare-Lse
region: eu-central-1
cloudProvider: aws
sdk:
  language: js
  path: ../client/src/sdk
frontend:
  path: ../client/build
  subdomain: amber-many-puma
scripts:
  preFrontendDeploy: "cd ../client && npm install && npm run build"
classes:
  - path: controllerUserData.ts
    type: jsonrpc
    methods: []
